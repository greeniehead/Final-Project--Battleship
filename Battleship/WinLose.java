import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
 
/**
 * Write a description of class WinLose here.
 * 
 * @author (K.G.D) 
 * @version (a version number or a date)
 */
public class WinLose extends Actor
{
  /*
    public void act() 
    {
     // if game is in grid form boolean[][] hitgrid = false; 
     AIhitter();
     hitter();
  }
  
  /*
    public void hitter()
    {
    //If statement to take player to win screen if AI ships are hit (17 times)
     if (canSee(Hit.class))
       {
          hit(Hit.class);
          hitShip++;
          if (hitShip == 17){
               WinScreen win = new Winscreen();
               Greenfoot.setWorld(win);
          }  
    }    
  }
  
   public void AIhitter()
   {
    //If statement to take player to lose screen if player ships are hit (17 times)
     if (canSee(AIFire.class))
       {
           AIhit(AIFire.class);
           AIhitShip++;
           if (AIhitShip == 17){
                LossScreen loss = new LossScreen();
                Greenfoot.setWorld(loss);
           } 
    }    
  }
  */
}
 
 
